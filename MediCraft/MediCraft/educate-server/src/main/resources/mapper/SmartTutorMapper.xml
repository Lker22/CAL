<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.education.tutor.mapper.SmartTutorMapper">

    <!-- 分页查询答疑历史 -->
    <select id="selectHistoryPage" resultType="com.education.vo.TutorAnswerVO">
        SELECT
            id as recordId,
            session_id as sessionId,
            question,
            text_answer as textAnswer,
            image_url as imageUrl,
            create_time as createTime
        FROM smart_tutor
        WHERE user_id = #{userId} AND deleted = 0
        ORDER BY create_time DESC
    </select>

    <!-- 查询答疑详情 -->
    <select id="selectDetailById" resultType="com.education.vo.TutorAnswerVO">
        SELECT
            id as recordId,
            session_id as sessionId,
            question,
            text_answer as textAnswer,
            image_url as imageUrl,
            video_url as videoUrl,
            create_time as createTime
        FROM smart_tutor
        WHERE id = #{id} AND user_id = #{userId} AND deleted = 0
    </select>

</mapper>