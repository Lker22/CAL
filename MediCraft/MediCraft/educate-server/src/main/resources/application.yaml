server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/MediCraft?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 822032abc
  ai:
    dashscope:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: qwen3-vl-plus
    openai: # openai 配置
      base-url: https://dashscope.aliyuncs.com/compatible-mode # 阿里百炼 api 地址
      #      base-url: https://api.chatanywhere.tech # api地址
      api-key: ${ALIYUN_API_KEY} # 读取环境变量中的阿里百炼 api key
      #      api-key: ${OPENAI_API_KEY} # 读取环境变量中的api key
      chat:
        options:
          model: qwen-plus #阿里百炼 模型名称
        enabled: false # 关闭 OpenAI 配置
  #          model: gpt-3.5-turbo # 模型名称
logging:
  level:
    com:
      sky:
        mapper: debug
        service: info
        controller: info

education:
  jwt:
    user-secret-key: AIStudySystem123456789987654321   # 原来硬编码的密钥
    user-ttl: 7200000                                   # 2小时 = 2*60*60*1000
    user-token-name: Authorization
