# MediCraft — AI 智能学习平台 项目文档

---

## 一、项目简介

MediCraft 是一个基于多智能体协同的个性化学习平台，旨在为学生提供从画像构建、资源生成、路径规划到效果评估的全链路 AI 辅助学习体验。系统通过 6 个专业 AI 智能体协作，根据学生的学习画像和目标，自动生成个性化学习资源、规划学习路径、提供智能答疑，并持续评估学习效果。

**项目架构**：前后端分离（单体后端 + Vue3 前端）

---

## 二、功能模块

### 1. 用户与学习画像模块

| 功能 | 说明 |
|---|---|
| 注册/登录 | JWT 认证，Token 有效期 24 小时 |
| 画像构建 | 与 AI 对话式收集 6 个维度：知识基础、认知风格、学习目标、易错点、学习节奏、资源偏好 |
| 画像展示 | 仪表盘展示画像完善度和各维度数据 |
| 画像查询 | 查看和编辑已有画像信息 |

### 2. AI 多智能体资源生成模块

| 智能体 | 角色 | 生成内容 |
|---|---|---|
| 需求解析智能体 | demand | 学习目标拆解、知识清单、推荐路径 |
| 文档生成智能体 | document | 结构化学习文档和笔记 |
| 思维导图智能体 | mind | 知识点思维导图 |
| 题库智能体 | question | 练习题和测试卷 |
| 实操案例智能体 | case | 实操案例和项目任务 |
| 多模态智能体 | multimodal | 视频脚本、音频讲解 |

生成流程：选择智能体 → 输入主题 → 异步生成（实时进度轮询） → 查看/下载结果

### 3. 学习路径规划与智能推送模块

| 功能 | 说明 |
|---|---|
| 一键生成路径 | 根据学习主题、目标、周期、强度，AI 自动生成 4-8 个学习步骤 |
| 路径详情 | 时间线展示步骤，支持打卡完成 |
| 动态调整 | 5 种调整方式：延长周期、压缩周期、调整顺序、新增内容、删除步骤 |
| 资源推荐 | 按学习主题模糊匹配资源，基于标题/知识点/课程名相关性评分 |

### 4. 智能辅导答疑模块

| 功能 | 说明 |
|---|---|
| AI 问答 | 输入问题（支持图片），AI 返回详细解答 |
| 逐字显示 | 模拟打字机效果展示 AI 回复 |
| 历史记录 | 查看、删除历史答疑记录 |

### 5. 学习效果评估模块

| 功能 | 说明 |
|---|---|
| AI 评估报告 | 根据学习行为数据生成多维度评估（学习概况、知识点掌握度 JSON、薄弱点分析、提升建议） |
| 评估结果 | 知识掌握分布仪表盘、知识点详情列表、薄弱点分析、能力雷达图 |
| 数据统计 | 总学习时长、答题正确率、每日趋势、资源类型分布、各学科学习进度 |

### 6. 定时任务

- 每小时自动同步评估结果到学生画像
- 自动标记薄弱知识点

---

## 三、技术栈

### 后端

| 技术 | 版本 | 用途 |
|---|---|---|
| Spring Boot | 3.3.5 | 应用框架 |
| Java | 17 | 编程语言 |
| Spring AI | 1.0.0-M6 | AI 框架（OpenAI 兼容接口） |
| MyBatis-Plus | 3.5.11 | ORM 框架 |
| MySQL | 8.0+ | 关系型数据库 |
| Redis | 6.0+ | 对话上下文缓存 |
| Fastjson2 | - | JSON 解析 |
| JWT | - | 用户认证 |
| Lombok | - | 代码简化 |

### 前端

| 技术 | 版本 | 用途 |
|---|---|---|
| Vue | 3 | 前端框架 |
| Vite | 5 | 构建工具 |
| Element Plus | - | UI 组件库 |
| Pinia | - | 状态管理 |
| Axios | - | HTTP 请求 |
| Vue Router | - | 前端路由 |

### AI 模型

- 支持 OpenAI 兼容接口（DashScope 千问、小米 MiMo 等）
- 通过 `application.yaml` 配置切换模型

---

## 四、架构亮点

### 1. 多智能体策略模式

```
AgentStrategyFactory → 根据 agentRole 分发 → 对应的 AgentGenerateStrategy 实现
```

6 个智能体各自独立的策略类，新增智能体只需实现接口 + 注册到工厂，符合开闭原则。

### 2. 异步任务解耦

资源生成通过 `ResourceAsyncExecutor` 独立 Bean 处理，解决 Spring `@Async` 自调用失效问题。前端轮询任务状态，无需长连接。

### 3. AI Prompt 工程化

- 评估报告：结构化 Prompt 要求 AI 输出固定格式（学习概况 → 知识点 JSON → 薄弱点 → 建议）
- 路径生成：根据学习周期和强度动态调整 Prompt
- 路径调整：5 种调整类型各有独立的 Prompt 构建逻辑

### 4. 资源推荐模糊匹配

基于标题、知识点、课程名的多维度关键词匹配，按相关性动态评分，而非简单的精确匹配。

### 5. 前后端分离 + 统一错误处理

- 前端 Axios 拦截器统一处理 `Result.msg` 字段和 HTTP 状态码
- 后端 `Result` 类统一响应格式（code/msg/data）
- 错误信息透传到前端业务层

### 6. 用户画像对话式构建

通过多轮 AI 对话收集用户画像信息，自动抽取结构化字段保存到数据库，而非传统的表单填写方式。

---

## 五、数据库设计

共 14 张表：

| 表名 | 说明 |
|---|---|
| sys_user | 用户表 |
| student_profile | 学生学习画像 |
| chat_context | 对话上下文 |
| ai_agent | AI 智能体配置 |
| learning_resource | 多模态学习资源 |
| learning_path | 学习路径 |
| learning_path_step | 学习路径步骤 |
| learning_path_step_resource | 步骤-资源关联（多对多） |
| learning_behavior | 学习行为记录 |
| question_answer_record | 题库答题记录 |
| video_watch_progress | 视频观看进度 |
| learning_evaluate | 学习效果评估 |
| smart_tutor | 智能辅导答疑 |
| resource_generate_task | 资源生成任务 |

---

## 六、已知问题与后续优化

### 高优先级

| 问题 | 说明 | 建议方案 |
|---|---|---|
| 题库不能答题 | 题库智能体生成了题目和答案，但没有答题界面和评分功能 | 新增答题页面，记录到 `question_answer_record` 表 |
| 智能辅导无真实流式输出 | 前端用 `setInterval` 模拟打字效果，不是真正的流式响应 | 接入 SSE（Server-Sent Events）实现真正的流式输出 |
| AI 响应无超时保护 | Spring AI 的 `ChatClient` 没有配置超时，AI 服务挂起时后端线程永久阻塞 | 在 `OpenAiApi` 配置连接超时和读取超时 |

### 中优先级

| 问题 | 说明 | 建议方案 |
|---|---|---|
| 推送通知缺失 | 需求分析提到 RabbitMQ 异步推送，但未实现 | 引入 WebSocket 实现实时通知（新资源就绪、路径建议等） |
| Redis 缓存未充分利用 | 对话上下文用了 Redis，但画像、资源、统计数据都没缓存 | 对热点数据增加 Redis 缓存层 |
| 前后端字段名不一致 | 前端用 `learningGoals/learningRhythm/errorPoints`，后端用 `learningGoal/learningPace/errorPronePoints` | 统一命名规范，或通过 VO 层做字段映射 |
| 评估报告评分机制简陋 | 评分基于简单的公式（时长/10、行为×15），不够准确 | 引入加权算法，结合答题正确率、路径完成度、学习时长等多维度 |

### 低优先级

| 问题 | 说明 | 建议方案 |
|---|---|---|
| 缺少单元测试 | 整个项目没有测试代码 | 补充 Service 层和 Controller 层的单元测试 |
| .idea 文件提交到 Git | IDE 配置文件不应入库 | 添加到 `.gitignore` |
| dm/ 目录冗余 | 重复的开发文件副本 | 删除 |
| API Key 硬编码 | `application.yaml` 中有明文 API Key | 使用环境变量或配置中心 |
| 前端没有权限控制 | 所有路由都可以访问 | 增加路由守卫 + 角色权限校验 |
| 数据库连接池未调优 | 使用 HikariCP 默认配置 | 根据并发量调整连接池参数 |

---

## 七、项目结构

```
MediCraft/                          # 后端项目根目录
├── educate-common/                 # 通用模块（JWT、工具类、拦截器）
├── educate-pojo/                   # 实体类和 VO
│   └── src/main/java/com/education/
│       ├── entity/                 # 14个实体类
│       ├── vo/                     # 请求/响应 VO
│       └── dto/                    # 数据传输对象
├── educate-server/                 # 核心服务模块
│   └── src/main/java/com/education/
│       ├── path/                   # 学习路径模块
│       ├── resource/               # 资源生成 + AI 配置
│       ├── user/                   # 用户 + 画像
│       ├── outcome/                # 评估 + 定时任务
│       ├── tutor/                  # 智能辅导
│       └── config/                 # 配置类
└── MediCraft.sql                   # 数据库建表脚本

Media-edu/MediCraft-web/            # 前端项目
└── src/
    ├── api/                        # API 接口层
    ├── stores/                     # Pinia 状态管理
    ├── views/                      # 页面视图
    │   ├── user/                   # 登录、注册、画像
    │   ├── ai-resource/            # 智能体、资源生成
    │   ├── learning-path/          # 路径规划
    │   ├── intelligent-tutor/      # 智能辅导
    │   └── learning-assessment/    # 评估统计
    ├── components/                 # 公共组件
    ├── router/                     # 路由配置
    └── utils/                      # 工具函数
```

---

## 八、快速启动

```bash
# 1. 准备环境
#    - JDK 17+
#    - Node.js 18+
#    - MySQL 8.0+
#    - Redis 6.0+

# 2. 初始化数据库
mysql -u root -p < MediCraft.sql

# 3. 修改后端配置
#    编辑 educate-server/src/main/resources/application.yaml
#    修改: 数据库密码、AI API Key、Redis 地址

# 4. 启动后端
cd MediCraft/MediCraft
mvn clean package -DskipTests
java -jar educate-server/target/educate-server-1.0-SNAPSHOT.jar

# 5. 启动前端
cd Media-edu/MediCraft-web
npm install
npm run dev
# 访问 http://localhost:5173
```

### 部署到其他机器需要修改的配置

| 配置项 | 文件位置 | 说明 |
|---|---|---|
| 数据库地址/密码 | `application.yaml` → `spring.datasource` | MySQL 连接信息 |
| AI API Key | `application.yaml` → `spring.ai.dashscope.api-key` | 模型密钥 |
| AI 模型/地址 | `application.yaml` → `spring.ai.dashscope.base-url/model` | 切换模型 |
| Redis 地址 | `application.yaml` → `spring.data.redis` | Redis 连接 |
| 前端代理 | `vite.config.js` → `proxy.target` | 后端地址（非本机时改） |
